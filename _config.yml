# Site settings
title: wintron.io
tag_text: wintron
description: I program my home computer. Beam myself into the future

# Build settings
markdown: kramdown
exclude: ["README.md"]
sass:
  style: compressed
plugins:
  - jekyll-redirect-from
